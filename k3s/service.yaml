apiVersion: v1
kind: Service
metadata:
  name: cut-url
  namespace: cut-url
  labels:
    app: cut-url
spec:
  type: NodePort
  selector:
    app: cut-url
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30090
      protocol: TCP
